{
  "name": "PhotoGroove",
  "version": "1.0.0",
  "scripts": {
    "start": "elm",
    "build": "elm make src/PhotoGroove.elm --optimize --output app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1-revision2"
  }
}
